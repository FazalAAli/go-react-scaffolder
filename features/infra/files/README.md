# {{project}}

Go + Echo + ConnectRPC backend and a React Router (SPA) frontend, in a Nix devshell.

## Quick start

```bash
direnv allow      # or: nix develop
just install      # frontend deps
just dev          # backend (:8000) + frontend
```

## Tasks

```bash
just            # list tasks
just gen        # regenerate Go + TS from proto/
just build      # build both
```

Generated by `scaffold`. Enabled features are recorded in `scaffold.toml`.
